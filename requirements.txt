# Core AI / RAG (optional — install if using AI Assistant module)
langchain>=0.1.0
openai>=1.0.0
anthropic>=0.20.0
chromadb>=0.4.0
pinecone-client>=3.0.0

# PDF generation (optional — for report exports)
weasyprint>=60.0

# XLSX export (for audit-ready equity & compliance exports)
openpyxl>=3.1.0

# Data utilities
pandas>=2.0.0
numpy>=1.24.0

# Auth helpers
python-jose[cryptography]>=3.3.0
PyJWT>=2.8.0
